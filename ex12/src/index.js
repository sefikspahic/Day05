function main() {
  // Only change code below this code

  var last-name = "Tesla";
  var lastLetterOfLastName = last-name[last-name.length - 1];

  // Only change code above this code
  return lastLetterOfLastName;
}

console.log(main());
module.exports = main;
